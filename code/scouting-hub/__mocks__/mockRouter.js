// mockRouter.js
import React from 'react';

const mockRouter = {
  useHistory: () => ({}),
  useLocation: () => ({}),
  useParams: () => ({}),
  useRouteMatch: () => ({})
};

export default mockRouter;
